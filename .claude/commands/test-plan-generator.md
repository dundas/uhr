Run the Windsurf workflow /test-plan-generator.
If not in Windsurf, follow `skills/test-plan-generator/SKILL.md` to generate an E2E test plan from a PRD, task list, or description. Requires explicit environment confirmation (Local/Staging/Production). Saves to `/tasks/testplan-[source].md`.
