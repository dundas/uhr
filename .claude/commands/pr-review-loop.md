Run the Windsurf workflow /pr-review-loop.
If not in Windsurf, follow `skills/pr-review-loop/SKILL.md` to monitor a PR for code review, analyze feedback with AI, implement fixes, and merge when approved with CI green.

Usage: `/pr-review-loop [PR-number]`

Example: `/pr-review-loop 42`
